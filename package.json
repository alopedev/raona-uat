{
  "name": "raona-uat-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude test/e2e/**",
    "test:e2e": "playwright test",
    "test:watch": "vitest --exclude test/e2e/**"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "exceljs": "^4.4.0",
    "vitest": "^4.1.0"
  }
}
